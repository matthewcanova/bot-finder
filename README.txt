Matthew Canova
Machine Learning Fall 2017

The code is written for Python 3.5.

The primary driver is bot_finder.py which uses classifier.py and decision_tree.py.

It is currently set up to execute the Bagged Forests when executed. Other sections can be uncommented as desired. 